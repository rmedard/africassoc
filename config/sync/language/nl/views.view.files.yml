display:
  default:
    display_options:
      exposed_form:
        options:
          submit_button: Filter
          reset_button_label: Reset
      fields:
        filename:
          label: Naam
        filemime:
          label: MIME-type
      filters:
        filemime:
          expose:
            label: MIME-type
